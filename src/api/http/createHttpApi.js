/**
 * MERN integration stub — implement with `fetch` or axios against your Express API.
 *
 * Return the same shape as `src/mocks/in-memory/mockApi.js`:
 * `{ auth, entities, integrations }`
 *
 * Wire this in `src/api/client.js` when `VITE_API_MODE === 'http'`.
 */
export function createHttpApi() {
  throw new Error(
    "createHttpApi is not implemented. Point it at your Express base URL and auth (e.g. JWT)."
  );
}
