# Routes live under `src/views/patient/`

- **`src/views/patient/pages`** — member / patient-facing screens (dashboard home, health, workouts, billing, etc.).

`pages.config.js` at the project root registers components and `mainPage`.
